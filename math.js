// math.js
// const add = (a, b) => a + b;
// const multiply = (a, b) => a * b;

// export { add, multiply };

export function add(x, y) {
    return x + y;
  }
  
  export function multiply(x, y) {
    return x * y;
  }